apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    application: matching-service
  ports:
    - name: http
      port: 80
      targetPort: 8000